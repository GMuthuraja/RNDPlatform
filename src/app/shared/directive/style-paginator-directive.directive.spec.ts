import { StylePaginatorDirectiveDirective } from './style-paginator-directive.directive';

describe('StylePaginatorDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new StylePaginatorDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
